import blockchain

class Block:
	def __init__(self, previousHash, lista):
		##set

		self._previousHash = previousHash
		self._timestamp =
		self.listOfTransactions = lista
		self.nonce = 

	def myHash:
		#calculate self.hash


	def add_transaction(self , transaction):
		#add a transaction to the block